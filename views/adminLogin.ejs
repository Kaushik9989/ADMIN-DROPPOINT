<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Authentication | DropPoint</title>
  
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --primary-orange: #ff6b00;
      --deep-black: #0f172a; /* Sophisticated midnight black */
      --soft-black: #1e293b;
      --text-gray: #94a3b8;
    }

    body {
      background-color: var(--deep-black);
      font-family: 'Inter', sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0;
      /* Subtle radial glow to add depth */
      background: radial-gradient(circle at center, #1e1b4b 0%, #0f172a 100%);
    }

    .login-card {
      background: #1e293b;
      border: 1px solid rgba(255, 107, 0, 0.2);
      border-radius: 20px;
      width: 100%;
      max-width: 400px;
      padding: 40px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
      position: relative;
      overflow: hidden;
    }

    /* Top orange accent line */
    .login-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: var(--primary-orange);
    }

    .brand-icon {
      width: 60px;
      height: 60px;
      background: rgba(255, 107, 0, 0.1);
      border: 1px solid var(--primary-orange);
      color: var(--primary-orange);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin: 0 auto 20px;
    }

    .card-title {
      color: #ffffff;
      font-weight: 700;
      text-align: center;
      margin-bottom: 8px;
    }

    .card-subtitle {
      color: var(--text-gray);
      text-align: center;
      font-size: 0.85rem;
      margin-bottom: 30px;
    }

    /* Form Styling */
    .form-label {
      color: #e2e8f0;
      font-size: 0.8rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .input-group-text {
      background: #0f172a;
      border: 1px solid #334155;
      color: var(--text-gray);
      border-right: none;
    }

    .form-control {
      background: #0f172a;
      border: 1px solid #334155;
      color: #ffffff;
      padding: 12px;
      font-size: 0.9rem;
    }

    .form-control:focus {
      background: #0f172a;
      border-color: var(--primary-orange);
      color: #ffffff;
      box-shadow: 0 0 0 4px rgba(255, 107, 0, 0.1);
    }

    .form-control::placeholder {
      color: #475569;
    }

    /* Buttons */
    .btn-login {
      background: var(--primary-orange);
      border: none;
      color: white;
      font-weight: 700;
      padding: 14px;
      border-radius: 10px;
      transition: all 0.3s ease;
      margin-top: 10px;
    }

    .btn-login:hover {
      background: #e66000;
      transform: translateY(-2px);
      box-shadow: 0 10px 15px -3px rgba(255, 107, 0, 0.3);
    }

    .btn-home {
      color: var(--text-gray);
      text-decoration: none;
      font-size: 0.85rem;
      text-align: center;
      display: block;
      margin-top: 20px;
      transition: color 0.2s;
    }

    .btn-home:hover {
      color: var(--primary-orange);
    }

    /* Error Alert */
    .alert-danger {
      background: rgba(239, 68, 68, 0.1);
      border: 1px solid #ef4444;
      color: #f87171;
      font-size: 0.85rem;
      border-radius: 10px;
    }

    .animate-in {
      animation: slideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }

    @keyframes slideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .brand-icon img {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain; /* Keeps the logo proportional within the box */
  display: block;
}

/* Optional: If you want the container to be slightly larger for a horizontal logo */
.brand-icon {
  width: auto; /* Allow width to grow with logo */
  min-width: 60px;
  max-width: 180px; /* Limits the logo from getting too wide */
  height: 60px;
  padding: 8px; /* Gives the logo some breathing room */
  background: rgba(255, 107, 0, 0.1);
  border: 1px solid var(--primary-orange);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 20px;
}
  </style>
</head>
<body>

  <div class="login-card animate-in">
    <div class="brand-icon">
      <img src="/uploads/droppointlogo.png">
    </div>
    
    <h3 class="card-title">DropPoint Admin</h3>
    <p class="card-subtitle">Authorize your session to continue</p>

    <% if (error) { %>
      <div class="alert alert-danger" role="alert">
        <i class="fas fa-circle-exclamation me-2"></i><%= error %>
      </div>
    <% } %>

    <form action="/admin/login" method="POST">
      <div class="mb-3">
        <label class="form-label">Username</label>
        <div class="input-group">
          <span class="input-group-text"><i class="fas fa-user-shield"></i></span>
          <input type="text" class="form-control" name="username" placeholder="Enter admin ID" required>
        </div>
      </div>

      <div class="mb-4">
        <label class="form-label">Password</label>
        <div class="input-group">
          <span class="input-group-text"><i class="fas fa-key"></i></span>
          <input type="password" class="form-control" name="password" placeholder="••••••••" required>
        </div>
      </div>

      <div class="d-grid">
        <button type="submit" class="btn btn-login">
          Access Console <i class="fas fa-arrow-right ms-2"></i>
        </button>
      </div>
    </form>


  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>