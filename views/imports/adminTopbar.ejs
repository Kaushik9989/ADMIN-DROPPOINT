<style>
  .topbar {
    position: fixed;
    top: 0;
    left: 260px; /* same as sidebar width */
    right: 0;
    height: 64px;
    background: rgba(30, 41, 59, 0.85); /* slate-900 glass */
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255,255,255,0.06);
    z-index: 1040;
    display: flex;
    align-items: center;
    padding: 0 24px;
  }

  .topbar-brand {
    font-weight: 700;
    color: #fff;
    letter-spacing: -0.3px;
    display: flex;
    align-items: center;
    text-decoration: none;
  }

  .topbar-brand i {
    color: #6366f1;
    margin-right: 10px;
    font-size: 1.1rem;
  }

  .topbar-right {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .admin-pill {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.08);
    padding: 6px 14px;
    border-radius: 999px;
    color: #e5e7eb;
    font-size: 0.85rem;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .admin-pill i {
    color: #818cf8;
  }

  .logout-btn-topbar {
    background: rgba(239,68,68,0.12);
    color: #f87171;
    border: 1px solid rgba(239,68,68,0.25);
    padding: 6px 14px;
    border-radius: 10px;
    font-weight: 600;
    font-size: 0.85rem;
    transition: all 0.2s ease;
    text-decoration: none;
  }

  .logout-btn-topbar:hover {
    background: #ef4444;
    color: #fff;
    border-color: #ef4444;
  }

  @media (max-width: 992px) {
    .topbar {
      left: 0;
    }
  }
</style>

<!-- Modern Topbar -->
<div class="topbar">
  <a href="/admin/dashboard" class="topbar-brand">
   
   
  </a>

  <div class="topbar-right">
    <div class="admin-pill">
      <i class="fa-solid fa-user-shield"></i>
      <span>Admin</span>
    </div>

    <a href="/admin/logout" class="logout-btn-topbar">
      <i class="fa-solid fa-right-from-bracket me-1"></i>
      Logout
    </a>
  </div>
</div>
